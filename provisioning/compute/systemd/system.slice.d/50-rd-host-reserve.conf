# Installed to /etc/systemd/system/system.slice.d/ by install.sh.
# Reserves memory and boosts scheduling weight for platform services
# (dockerd, sshd, CTFd infra) so they win contention against rd.slice.

[Slice]
MemoryMin=2G
CPUWeight=500
IOWeight=500
