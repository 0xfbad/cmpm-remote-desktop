# Docker must never start (including via docker.socket activation or a manual
# start) unless the remote-desktop nft policy has loaded successfully.
[Unit]
Requires=rd-network-policy.service
After=rd-network-policy.service
