# RUNNER-ONLY reference for the default data-root. install.sh
# --with-storage-opts generates this drop-in with Docker's active data-root.
# Keeps dockerd DOWN AND LOUD when the XFS data-root mount is missing,
# instead of silently running unquota'd on the root filesystem.
[Unit]
RequiresMountsFor=/var/lib/docker
