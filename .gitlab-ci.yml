check-docker:
  image: hadolint/hadolint:latest-debian
  script:
    - hadolint Dockerfile

check-shell:
  image: koalaman/shellcheck-alpine:latest
  script:
    - shellcheck configs/*.sh install/*.sh
